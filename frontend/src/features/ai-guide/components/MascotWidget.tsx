/**
 * STUB — the future AI guide mascot.
 *
 * Deliberately not implemented in this pass, matching ai-agent.service.ts on the
 * backend. It renders nothing and is not mounted anywhere; the file exists so the
 * intended seam is visible without any half-built behaviour shipping.
 *
 * See docs/REMAINING-WORK.md.
 */
export function MascotWidget(): null {
  return null;
}
